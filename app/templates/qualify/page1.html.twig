{% extends "base.html.twig" %}

{% block title %}{{ parent() }} - Votre demande préalable de travaux{% endblock %}

{% block content %}
<main>

    <section class="section-color questions-container">
        <h4>Dans quelle ville allez-vous réaliser des travaux ? (1/2)</h4>

        {% if villes != null %}
                <ul>
                    {% for ville in villes %}
                        <a href="{{ path("route_qualify2", {ville_id: ville.id}) }}">
                            <li>{{  ville.nom }}</li>
                        </a>
                    {% endfor %}
                </ul>
        {% else %}
            <p>
                Il n'y a pas de villes partenaires pour le moment. Revenez dans quelque temps !
            </p>
        {% endif %}
    </section>

    <section>
        <h6>Votre ville n'est pas dans cette liste ?</h6>
        Alors, elle n'est pas partenaire pour le moment. Vous pouvez utiliser la voie traditionnelle et déposer en mairie votre déclaration d'urbanisme que vous pourrez imprimer depuis
        <a href="https://www.service-public.fr/particuliers/vosdroits/F17578">service-public.fr</a>.
        </p>

    </section>

</main>
{% endblock %}
